<div class="<%= dashboard_classes %>">
  <!-- Page Header -->
  <%= render(Layout::Dashboard::HeaderComponent.new(**header_data)) %>

  <!-- Statistics Cards -->
  <div class="mt-4">
    <h2 class="f4 text-semibold mb-3">System Overview</h2>
    <div class="d-flex flex-wrap" style="gap: 16px;">
      <% statistics_data.each do |stat| %>
        <div class="flex-1" style="min-width: 240px;">
          <%= render(Layout::Dashboard::StatsCardComponent.new(
            title: stat[:title],
            value: stat[:value],
            icon: stat[:icon],
            color: stat[:color],
            subtitle: stat[:subtitle]
          )) %>
        </div>
      <% end %>
    </div>
  </div>

  <!-- Content Sections -->
  <div class="mt-5">
    <div class="d-lg-flex" style="gap: 24px;">
      <!-- Users by Role -->
      <div class="flex-1 mb-4 mb-lg-0">
        <%= render(Layout::Dashboard::DataGridComponent.new(
          title: users_by_role_data[:title],
          data: users_by_role_data[:data],
          manage_path: users_by_role_data[:manage_path],
          manage_text: users_by_role_data[:manage_text],
          empty_state: users_by_role_data[:empty_state],
          icon: "person"
        )) %>
      </div>

      <!-- Documents by Status -->
      <div class="flex-1">
        <%= render(Layout::Dashboard::DataGridComponent.new(
          title: documents_by_status_data[:title],
          data: documents_by_status_data[:data],
          manage_path: documents_by_status_data[:manage_path],
          manage_text: documents_by_status_data[:manage_text],
          empty_state: documents_by_status_data[:empty_state],
          icon: "file"
        )) %>
      </div>
    </div>
  </div>

  <!-- Recent Activity Section -->
  <div class="mt-5">
    <% activity_data = recent_activity_data.dup %>
    <% activity_data[:view_all_path] = send(activity_data[:view_all_path], current_user) %>
    <%= render(Layout::Dashboard::RecentActivityComponent.new(**activity_data)) %>
  </div>

  <!-- Quick Actions Section -->
  <div class="mt-5">
    <%= render(Layout::Dashboard::QuickActionsComponent.new(
      actions: quick_actions_data
    )) %>
  </div>
</div>
