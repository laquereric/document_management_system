<div class="<%= container_classes %>">
  <i class="octicon octicon-<%= icon %> f1 color-fg-muted"></i>
  <h4 class="mt-3 mb-2"><%= title %></h4>
  <p class="color-fg-muted mb-4"><%= description %></p>
  
  <% if has_action? %>
    <%= link_to action_text_value, action_url_value, class: action_classes %>
  <% end %>
</div>
