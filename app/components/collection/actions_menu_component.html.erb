<!-- Generic actions menu using Primer components -->
<%= render Primer::Alpha::ActionMenu.new do |menu| %>
  <% menu.with_trigger_button(icon: :"kebab-horizontal", "aria-label": "#{resource_type.humanize} actions") %>
  
  <% menu_items.each do |item| %>
    <% if item[:href] == "#" %>
      <%= menu.with_item(
        href: item[:href],
        data: item[:data] || {},
        target: item[:target],
        classes: item[:classes]
      ) do %>
        <%= render Primer::Beta::Octicon.new(icon: item[:icon], mr: 2) %>
        <%= item[:label] %>
      <% end %>
    <% else %>
      <%= menu.with_item(
        href: item[:href],
        data: item[:data] || {},
        target: item[:target],
        classes: item[:classes]
      ) do %>
        <%= render Primer::Beta::Octicon.new(icon: item[:icon], mr: 2) %>
        <%= item[:label] %>
      <% end %>
    <% end %>
  <% end %>
<% end %>
